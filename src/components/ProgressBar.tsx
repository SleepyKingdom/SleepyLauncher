import React from 'react';

const ProgressBar: React.FC = () => (
  <div className="w-64 bg-gray-200 rounded">
    <div className="h-2 bg-blue-500" style={{ width: '50%' }} />
  </div>
);

export default ProgressBar;
